'use strict';

/**
 * join-the-family-card service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::join-the-family-card.join-the-family-card');
